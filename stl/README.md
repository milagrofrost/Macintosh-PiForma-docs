# STL Files

Place final STL files in the matching folders.

Suggested structure:

```text
stl/
├── shelf/
├── bezel/
├── screen-feet/
├── power-rail/
├── battery-plunger/
├── boot-chime-switch/
└── monitor-stand/
```

The Apple-style mouse shell is currently an external design. Add the source link under `references/` once found.
